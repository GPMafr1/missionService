version: '2'
services:
  missionservice-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/missionService/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=missionService
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
